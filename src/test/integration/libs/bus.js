'use strict'

module.exports = (opts) => {
  // const assert = opts.assert

  // describe('#transform', function () {
  //   it('should work', async function () {
  //     this.timeout(5000)

  //     opts.bus.inQueueData = [{
  //       id: 1,
  //       description: 'Test'
  //     }]

  //     await opts.bus.transform(
  //       'id',
  //       'inQueue',
  //       'outQueue',
  //       (payload, context, done) => {
  //         done(null, payload)
  //       }
  //     )

  //     assert.strictEqual(opts.bus.outQueueData.length, 1)
  //     assert.strictEqual(opts.bus.outQueueData[0].id, 'id')
  //     assert.strictEqual(opts.bus.outQueueData[0].payload.description, 'Test')
  //   })
  // })
}
