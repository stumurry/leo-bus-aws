{
  "config": {
    "leo": {
      "cron": {
        "settings": {}
      },
      "env": {
        "GIT_SECRET": "${GitSecret}",
        "GIT_URL": "${GitUrl}"
      },
      "memory": 128,
      "role": "ApiRole",
      "timeout": 300,
      "type": "cron",
      "useWrappers": false
    }
  },
  "description": "",
  "directories": {
    "test": "test"
  },
  "main": "index.js",
  "name": "VibeRefreshRequest",
  "scripts": {
    "test": "leo-cli test . "
  },
  "version": "1.1.0"
}