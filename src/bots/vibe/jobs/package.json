{
  "config": {
    "leo": {
      "cron": {
        "settings": {},
        "time": "0 */5 * * * * "
      },
      "env": {
        "GIT_SECRET": "${GitSecret}",
        "GIT_URL": "${GitUrl}"
      },
      "memory": 128,
      "role": "ApiRole",
      "timeout": 300,
      "type": "cron"
    }
  },
  "description": "",
  "directories": {
    "test": "test"
  },
  "main": "index.js",
  "name": "VibeJobs",
  "scripts": {
    "test": "leo-cli test . "
  },
  "version": "1.1.0"
}