{
  "config": {
    "leo": {
      "cron": {
        "settings": {
          "destination": "mapped-tree-nodes",
          "source": "plmb-utc-new-tree-nodes"
        }
      },
      "env": {
        "GIT_SECRET": "${GitSecret}",
        "GIT_URL": "${GitUrl}"
      },
      "memory": 256,
      "role": "ApiRole",
      "timeout": 300,
      "type": "cron"
    }
  },
  "description": "",
  "directories": {
    "test": "test"
  },
  "main": "index.js",
  "name": "MapTreeNodeBot",
  "scripts": {
    "test": "leo-cli test . "
  },
  "version": "1.1.0"
}