{
  "config": {
    "leo": {
      "cron": {
        "settings": {}
      },
      "env": {
        "GIT_SECRET": "${GitSecret}",
        "GIT_URL": "${GitUrl}"
      },
      "memory": 128,
      "role": "ApiRole",
      "timeout": 300,
      "type": "cron"
    }
  },
  "description": "Pulls health check reports from SNS",
  "directories": {
    "test": "test"
  },
  "main": "index.js",
  "name": "LoadSNSHealthCheckFailed",
  "scripts": {
    "test": "leo-cli test . "
  },
  "version": "1.1.0"
}